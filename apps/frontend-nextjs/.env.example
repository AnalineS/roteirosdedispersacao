# =======================================================
# ENVIRONMENT CONFIGURATION EXAMPLE
# =======================================================
# All actual values are set via GitHub Actions variables/secrets
# No local .env files needed in production

# Backend API URLs (set via GitHub variables)
# NEXT_PUBLIC_API_URL_STAGING=https://hml-roteiro-dispensacao-api-xxx.run.app/api/v1
# NEXT_PUBLIC_API_URL_PRODUCTION=https://roteiro-dispensacao-api-xxx.run.app/api/v1

# Environment Detection (set via GitHub variables)
# NEXT_PUBLIC_ENVIRONMENT=staging|production

# Feature Flags (set via GitHub variables)
# NEXT_PUBLIC_AUTH_ENABLED=true
# NEXT_PUBLIC_OFFLINE_MODE=true

# Privacy & Analytics (set via GitHub variables)
# NEXT_PUBLIC_ANALYTICS_ENABLED=false
# NEXT_PUBLIC_COOKIES_ENABLED=true

# =======================================================
# BACKEND OAUTH CONFIGURATION (GitHub Secrets)
# =======================================================
# These are configured in the backend via GitHub secrets:
# - GOOGLE_CLIENT_ID (HML and PROD versions)
# - GOOGLE_CLIENT_SECRET (HML and PROD versions)
# - SECRET_KEY (JWT signing key)
# - GCS_BUCKET_NAME (Cloud Storage bucket)

# =======================================================
# LOCAL DEVELOPMENT ONLY
# =======================================================
# For local development, copy this file to .env.local and set:
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
NEXT_PUBLIC_ENVIRONMENT=development
NEXT_PUBLIC_AUTH_ENABLED=true
NEXT_PUBLIC_OFFLINE_MODE=true
NEXT_PUBLIC_ANALYTICS_ENABLED=false
NEXT_PUBLIC_COOKIES_ENABLED=true